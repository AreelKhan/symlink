Read the current decision log at ai/decision-log.md. Determine the next decision number by looking at the last entry. Append a new row to the markdown table with:
- The next sequential decision number
- The decision itself (concise but complete)
- Why we made this decision (the reasoning and tradeoffs)
- Today's date

The log is append-only — never modify or remove existing entries. Only add new rows to the table.

The decision to log: $ARGUMENTS
